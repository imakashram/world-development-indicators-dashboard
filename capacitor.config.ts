import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'population_gdp_dashboard',
  webDir: 'www'
};

export default config;
